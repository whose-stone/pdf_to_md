# Local config — contains API keys. Template is at examples/config.toml.
/config.toml

# Local env file — contains API keys. Template is at .env.example.
.env

# Local PDF inputs and generated Markdown outputs
/input/
/output/

# PyInstaller build artifacts
/build/
/dist/

# Python bytecode cache
__pycache__/
*.pyc
